import { Select2boxDirective } from './select2box.directive';

describe('Select2boxDirective', () => {
  it('should create an instance', () => {
    const directive = new Select2boxDirective();
    expect(directive).toBeTruthy();
  });
});
